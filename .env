# Default provider if none is specified in code (UI can override this)
LLM_PROVIDER=mock

# --- OpenAI settings ---
# (keep this secret, do not commit .env to git)
OPENAI_API_KEY=sk-proj-XXXX
# Which OpenAI model to use by default
OPENAI_MODEL=gpt-4.1-mini

# --- Hugging Face Inference settings ---
HF_API_KEY=hf_XXXX
HF_MODEL=openai/gpt-oss-20b